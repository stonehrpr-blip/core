# _REVIEW_DELETE — archived onboarding-era previews

These are the **old onboarding/coach-flow prototype screens** (morning-brief,
bedtime, weekly-review, stat-wizards, scan-*, coach-*, paywall variants, etc.),
moved here on 2026-06-04 when the preview suite was reworked into the MMORPG
progression product (`20-dashboard.html` + life scores, quests, inventory,
shop, ranks, streak, profile).

**Status:** pending review → deletion. Kept only so nothing is lost while the
new suite stabilises. They are **not linked** from any live screen.

**Do not resurrect or edit these** — if you need a pattern from one, copy it
into a current screen rather than reviving the file. Once the team confirms
nothing here is still needed, this whole folder can be `git rm`'d (the history
is preserved either way).

The current, live preview suite lives one level up in `previews/`.
