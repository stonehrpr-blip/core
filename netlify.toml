# Root Netlify config — publish the static HTML previews, no build step.
# Overrides Netlify's Expo auto-detection (which tries `expo export -p web`).
[build]
  base = ""
  publish = "previews"
  command = "echo 'static previews — no build'"
