/rename Lane4-Scan-Tab

You are working in ~/Desktop/lifeos/apps/mobile (CORE app, Expo/React Native). Your lane is BUILDING THE SCAN TAB. It currently CRASHES: app/(tabs)/_layout.tsx declares a "scan" tab but app/(tabs)/scan.tsx doesn't exist and components/scanner/ is empty.

GOAL: Replace the dead Scan tab with a real, non-crashing screen.

YOU OWN (only edit these): new app/(tabs)/scan.tsx, components/scanner/*
DO NOT TOUCH: anything outside the scan feature.

STEPS:
1. Reference archived designs previews/_REVIEW_DELETE/36-scan-food.html, 37-scan-body.html, 38-scan-outfit.html for the intended UX.
2. Build an MVP scan HUB: Food / Body / Outfit cards, with camera-permission request copy. If the camera/AI capture pipeline isn't ready, each card shows an honest "Scanning rolls out soon" state — do NOT ship a fake/broken capture flow.
3. Privacy: camera/photo must be strictly opt-in and local-first (matches the app's Privacy Policy). No silent uploads.

DONE WHEN: tapping Scan renders a hub with no crash; permission copy is correct; no fake capture.
VERIFY: npx expo start — Scan tab loads, cards render, permission prompt copy is correct.
