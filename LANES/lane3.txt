/rename Lane3-Coach-Tab

You are working in ~/Desktop/lifeos/apps/mobile (CORE app, Expo/React Native). Your lane is BUILDING THE COACH TAB. It currently CRASHES: app/(tabs)/_layout.tsx declares a "coach" tab but app/(tabs)/coach.tsx doesn't exist and components/coach/ is empty.

GOAL: Replace the dead Coach tab with a real, non-crashing screen.

YOU OWN (only edit these): new app/(tabs)/coach.tsx, components/coach/*
DO NOT TOUCH: anything outside the coach feature.

STEPS:
1. Reference the archived design previews/_REVIEW_DELETE/39-coach.html (and 40/41-coach-craving) for layout and tone. Port the look to RN using the existing theme/colors.ts and shared components.
2. Build an MVP: chat UI (message list + input), the coach-orb visual (see 21-quests.html), and a "craving SOS" entry point. Respect the coach-tone setting (auth-store coachingStyle).
3. CRITICAL SAFETY RULE (Stone's standing preference): AI is confirmation-gated and takes NO autonomous actions — coach replies only, never auto-send/pay/delete/control. If the backend model call isn't ready, stub it in lib/ and render canned supportive responses so the tab never crashes.

DONE WHEN: tapping Coach renders a real screen with no crash; tone setting respected.
VERIFY: npx expo start — Coach tab loads, send a message, get a response.
